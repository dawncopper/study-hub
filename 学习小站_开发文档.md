---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 3633ca83b40dddc277e8ec78acdb8e95_80cedf81b10e11f1af37525400826444
    ReservedCode1: ZpIKkQa4gc+qiBsd4QxyU6cYBVnvCXvR0z/Xh5GRYsExY/+PQuIEz9f6cczmtCGcaMb0X0quMrG58iSuAScnQq2eiNNRFYE26jTGBEsxe6Uquwk+R8sv+DBJudysdlkQGmgbuOMtAK4U0y1kq2A5Y9A4AQ+HWo/8wTIdbR++kjZfoo+gj1aRWbAayUo=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 3633ca83b40dddc277e8ec78acdb8e95_80cedf81b10e11f1af37525400826444
    ReservedCode2: ZpIKkQa4gc+qiBsd4QxyU6cYBVnvCXvR0z/Xh5GRYsExY/+PQuIEz9f6cczmtCGcaMb0X0quMrG58iSuAScnQq2eiNNRFYE26jTGBEsxe6Uquwk+R8sv+DBJudysdlkQGmgbuOMtAK4U0y1kq2A5Y9A4AQ+HWo/8wTIdbR++kjZfoo+gj1aRWbAayUo=
---

# 学习小站（StudyHub）开发文档

版本：v1.0
更新日期：2026-09-15

---

## 1. 项目概述

### 1.1 背景与目标
给孩子和家长使用的「每日学习验证 + 激励」应用。核心目标：让孩子**不厌学、增加对知识的兴趣**，通过每日打卡和积分奖励机制，把学习变成有成就感的事。

### 1.2 使用场景
- 家长每天在系统中记录/确认孩子当天的学习情况（孩子不操作手机）
- 多位家长可共同参与（各自独立账号）
- 系统根据完成情况自动累计星星积分，孩子攒积分兑换游戏时间
- 家长线下把控游戏时间，系统只做记录与展示

### 1.3 用户画像
- **孩子**：初一，男生（具体信息可配置，系统应支持小学到高中）
- **家长**：2 位及以上，共同管理，账号可分工

### 1.4 核心功能清单
| 编号 | 模块 | 说明 |
|---|---|---|
| F1 | 每日打卡 | 校内作业完成情况、预习情况、课外练习完成情况，分科目记录 |
| F2 | 难点疑点记录 | 记录难点/疑点，可标记已解决 |
| F3 | 错题管理 | 拍照上传 + OCR 识别文字、按科目/知识点分类、标签筛选、导出错题本 PDF |
| F4 | 积分奖励 | 星星兑换制 + 扣减机制，规则参数化可修改 |
| F5 | 游戏时间管理 | 积分兑换分钟数，家长线下执行 |
| F6 | 数据统计 | 周报/月报、完成率、积分趋势 |
| F7 | 成长激励 | 成长树等级、成就勋章 |
| F8 | 多方参与 | 多家长独立账号，管理员管理 |
| F9 | 提醒推送 | 网页内提醒 + 企业微信/微信群机器人/邮件推送 |
| F10 | 孩子展示页 | 无需登录的成长树/勋章展示页（平板给孩子看） |

### 1.5 部署要求
- 部署环境：**群晖 NAS（Docker Compose 一键部署）**，兼容 Windows/Mac 本地运行
- 数据持久化：数据库、上传图片、导出文件均存于 `data/` 挂载卷

---

## 2. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Python 3.11 + FastAPI | 轻量、异步、自带 API 文档 |
| ORM | SQLAlchemy 2.x | 数据模型清晰 |
| 数据库 | SQLite（单文件） | 家庭单机场景足够，无需额外容器 |
| 前端 | Jinja2 服务端渲染 + 原生 JS + CSS | 单容器部署简单，无需构建步骤 |
| OCR | Tesseract 5 + chi_sim 语言包 | 开源、离线可用、Docker 内置 |
| PDF 导出 | ReportLab | 生成错题本 PDF |
| 定时任务 | APScheduler | 每日提醒 |
| 推送 | HTTP Webhook（企业微信/微信群）+ SMTP（邮件） | 家庭场景常用 |
| 认证 | Cookie Session（内存表） | 简单够用，家庭内网场景 |

### 2.1 依赖清单（requirements.txt）
```
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
sqlalchemy==2.0.30
python-multipart==0.0.9
pytesseract==0.3.10
Pillow==10.3.0
reportlab==4.2.0
apscheduler==3.10.4
requests==2.32.3
```

### 2.2 Docker 基础镜像要求
- 基础镜像：`python:3.11-slim`
- 系统包：`tesseract-ocr`、`tesseract-ocr-chi-sim`、`fonts-noto-cjk`（中文字体，PDF 导出必需）、`tzdata`
- 环境变量：`TZ=Asia/Shanghai`
- 启动命令：`uvicorn app.main:app --host 0.0.0.0 --port 8000`
- 端口：`8000`
- 卷挂载：`./data:/app/data`

---

## 3. 系统架构

```
┌─────────────────────────────────────────────┐
│              浏览器（家长端/孩子端）          │
└──────────────────┬──────────────────────────┘
                   │ HTTP (8000)
┌──────────────────▼──────────────────────────┐
│              FastAPI 应用（单容器）           │
│  ┌──────────┬──────────┬─────────────────┐  │
│  │ 认证/会话 │ 各功能路由 │ 定时提醒(APScheduler)│  │
│  └──────────┴──────────┴─────────────────┘  │
│  ├─ OCR 模块（Tesseract）                    │
│  ├─ PDF 导出模块（ReportLab）                │
│  └─ 推送模块（Webhook / SMTP）              │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  SQLite（data/studyhub.db） + 上传图片/导出  │
└─────────────────────────────────────────────┘
```

### 3.1 目录结构
```
studyhub/
├── app/
│   ├── __init__.py
│   ├── main.py              # 应用入口、登录、Dashboard、孩子端
│   ├── models.py            # SQLAlchemy 数据模型
│   ├── database.py          # SQLite 引擎与会话
│   ├── seed.py              # 首次启动初始化（管理员/科目/规则/设置）
│   ├── auth.py              # 会话登录
│   ├── ocr.py               # 错题 OCR 识别
│   ├── report.py            # 周报/月报数据 + 错题本 PDF 导出
│   ├── notify.py            # 企业微信/微信群/邮件推送
│   ├── routes_daily.py      # 每日打卡
│   ├── routes_difficulty.py # 难点疑点
│   ├── routes_errorbook.py  # 错题管理
│   ├── routes_rewards.py    # 积分奖励
│   ├── routes_stats.py      # 数据统计
│   ├── routes_settings.py   # 设置（规则/推送/账号/科目）
│   ├── templates/           # Jinja2 页面
│   │   ├── base.html        # 顶栏导航骨架
│   │   ├── login.html
│   │   ├── dashboard.html   # 今日概览
│   │   ├── daily.html       # 每日打卡
│   │   ├── difficulty.html  # 难点疑点
│   │   ├── errorbook.html   # 错题本
│   │   ├── rewards.html     # 积分奖励
│   │   ├── stats.html       # 数据统计
│   │   ├── settings.html    # 设置
│   │   └── kid.html         # 孩子端展示页（公开）
│   └── static/css/style.css # 全局样式
├── data/                    # 数据目录（挂载卷持久化）
├── Dockerfile
├── docker-compose.yml
└── requirements.txt
```

---

## 4. 数据库设计

数据库：SQLite 单文件。所有表通过 SQLAlchemy 声明式定义，首次启动自动建表。

### 4.1 parents（家长账号）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| username | String(50) UNIQUE | 登录名 |
| password_hash | String(200) | PBKDF2-SHA256 哈希（盐固定 studyhub） |
| nickname | String(50) | 昵称，如「爸爸」「妈妈」 |
| role | String(20) | `admin` / `member`，admin 可管理账号 |
| subjects | String(200) | 分工科目 ID（逗号分隔），空=全科（v1 仅存储展示，不做权限隔离） |
| created_at | DateTime | |

### 4.2 students（学生）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| name | String(50) | 默认「小明」 |
| gender | String(10) | 默认「男」 |
| grade | String(50) | 默认「初一」 |
| avatar | String(20) | emoji 头像，默认 🎒 |
| created_at | DateTime | |

### 4.3 subjects（科目）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| name | String(50) UNIQUE | 科目名 |
| color | String(20) | 标签色值 |
| sort_order | Integer | 排序 |

默认科目：语文、数学、英语、科学、道法、历史、地理、生物（可在设置增删）。

### 4.4 daily_records（每日打卡，覆盖 F1）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| record_date | Date | 打卡日期 |
| student_id | Integer FK | |
| category | String(20) | `homework`校内作业 / `preview`预习 / `extra`课外练习 |
| subject_id | Integer FK | |
| content | String(500) | 内容描述 |
| status | String(20) | `done`完成 / `partial`部分完成 / `undone`未完成 |
| quality | Integer | 质量星级 0-5 |
| note | String(300) | 备注 |
| parent_id | Integer FK | 登记人 |
| points_granted | Integer | 本次发放积分（修改/删除时回滚） |
| created_at | DateTime | |

唯一约束：`(record_date, category, subject_id)` —— 同一科目同类别同一天只能有一条。

### 4.5 difficulties（难点疑点，覆盖 F2）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| student_id | Integer FK | |
| subject_id | Integer FK | |
| title | String(200) | 标题 |
| content | Text | 详细说明 |
| status | String(20) | `open`未解决 / `solved`已解决 |
| solution | Text | 解决过程/讲解要点 |
| parent_id | Integer FK | |
| created_at | DateTime | |
| solved_at | DateTime | |

### 4.6 error_items（错题，覆盖 F3）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| student_id | Integer FK | |
| subject_id | Integer FK | |
| knowledge_point | String(100) | 知识点 |
| tags | String(200) | 标签，逗号分隔 |
| question_text | Text | OCR 识别出的题目 |
| answer_text | Text | OCR 识别出的答案 |
| error_reason | String(300) | 错因 |
| image_path | String(300) | 原图文件名（存 data/uploads/） |
| status | String(20) | `pending`待订正 / `reviewed`已订正 / `mastered`已掌握 |
| parent_id | Integer FK | |
| created_at | DateTime | |

### 4.7 reward_configs（积分规则，参数化，覆盖 F4）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| key | String(50) UNIQUE | 规则键 |
| name | String(100) | 规则名 |
| value | Integer | 分值（正=奖励，负=扣减） |
| unit | String(20) | 单位，默认「星」 |
| description | String(200) | 说明 |

**默认规则（上线后家长和孩子可随时修改）**：
| key | name | 默认值 | 说明 |
|---|---|---|---|
| points_homework | 完成校内作业 | +10 | 每日每科 |
| points_preview | 完成预习 | +5 | 每日每科 |
| points_extra | 完成课外练习 | +8 | 每日每科 |
| points_quality | 作业质量满分额外奖励 | +3 | 质量评 5 星时额外奖励 |
| points_difficulty | 难点/疑点解决 | +15 | 标记已解决 |
| points_errorbook | 录入一道错题 | +5 | 拍照录入 |
| points_error_mastered | 错题订正掌握 | +15 | 标记已掌握 |
| points_continuous7 | 连续打卡7天 | +30 | 周期奖励（v1 通过勋章体现） |
| deduct_undone | 未完成作业 | -5 | 每日每科 |
| deduct_partial | 作业部分完成 | -2 | 每日每科 |
| exchange_points | 兑换1分钟游戏时间所需星星 | 3 | 默认 3 星=1 分钟 |
| daily_game_limit | 每日游戏时间上限 | 60 | 分钟，家长线下把控参考 |
| weekly_game_limit | 每周游戏时间上限 | 180 | 分钟，家长线下把控参考 |

### 4.8 point_transactions（积分流水）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| student_id | Integer FK | |
| amount | Integer | 正=收入，负=支出 |
| category | String(20) | `task`任务奖励 / `reward`兑换消耗 / `deduct`扣减 / `adjust`人工调整 |
| reason | String(200) | 说明 |
| ref_id | Integer | 关联记录 ID |
| parent_id | Integer FK | 操作人 |
| created_at | DateTime | |

### 4.9 game_time_orders（游戏时间兑换单，覆盖 F5）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| student_id | Integer FK | |
| points_cost | Integer | 消耗星星 |
| minutes | Integer | 分钟数 |
| status | String(20) | `approved`待使用 / `used`已使用 |
| note | String(200) | 备注 |
| parent_id | Integer FK | |
| created_at | DateTime | |

### 4.10 badges（成就勋章，覆盖 F7）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| student_id | Integer FK | |
| badge_key | String(50) | 唯一键 |
| name | String(100) | 勋章名 |
| icon | String(20) | emoji |
| earned_at | DateTime | |

唯一约束：`(student_id, badge_key)`。

**勋章规则（自动检查）**：
| key | 名称 | 图标 | 条件 |
|---|---|---|---|
| streak7 | 连续打卡7天 | 🔥 | 连续 7 天有打卡记录 |
| streak30 | 连续打卡30天 | 🌟 | 连续 30 天 |
| err10 | 错题猎人 | 🎯 | 掌握错题 ≥10 |
| err50 | 错题大师 | 🏆 | 掌握错题 ≥50 |
| pt500 | 积分新星 | ⭐ | 累计积分 ≥500 |
| pt2000 | 积分达人 | 👑 | 累计积分 ≥2000 |

### 4.11 settings（系统设置，覆盖 F9）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer PK | |
| key | String(50) UNIQUE | |
| value | Text | |

默认键：
- `reminder_time`（默认 19:00）、`reminder_enabled`（默认 1）
- `notify_wecom_webhook`、`notify_wechat_webhook`（空=不启用）
- `notify_email_smtp`、`notify_email_port`（465）、`notify_email_user`、`notify_email_pass`、`notify_email_to`
- `student_name`、`student_grade`、`student_gender`（同步学生信息）
- `school_notice`（欢迎语）

---

## 5. 后端 API 设计

### 5.1 认证
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | / | 未登录跳 /login，已登录跳 /dashboard |
| GET | /login | 登录页 |
| POST | /login | 表单 username/password，成功写 cookie `studyhub_sid`（7天），失败回登录页带错误 |
| GET | /logout | 清除会话 |

会话机制：内存字典 token -> (parent_id, 过期时间)，cookie 存 token。中间件校验，未登录跳转 /login。

### 5.2 Dashboard（今日概览）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /dashboard | 今日各模块打卡记录、积分余额、未解决难点数、待订正错题数、兑换规则文案、近10条积分流水、未完成提醒横幅 |

### 5.3 每日打卡（F1）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /daily/ | d=日期(可选，默认今天) | 打卡页，按日期展示三类记录 |
| POST | /daily/add | d, category, subject_id, content, status, quality | 新增/更新（同日期同科目同类别存在则更新）；自动计算积分并写流水；更新时先回滚旧积分 |
| POST | /daily/delete/{id} | | 删除并回滚积分 |

积分计算逻辑：
- status=done：加 category 对应奖励分；homework 且 quality≥5 额外加 points_quality
- status=partial：扣 deduct_partial 的绝对值分（写负数）
- status=undone：扣 deduct_undone 的绝对值分

### 5.4 难点疑点（F2）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /difficulty/ | status=open/solved | 列表页 |
| POST | /difficulty/add | subject_id, title, content | 新增 |
| POST | /difficulty/solve/{id} | solution | 标记解决（open→solved，写 solved_at，发放 points_difficulty） |
| POST | /difficulty/reopen/{id} | | 重新打开 |
| POST | /difficulty/delete/{id} | | 删除 |

### 5.5 错题管理（F3）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /errorbook/ | subject_id, knowledge, tag, status | 列表 + 筛选，最多200条 |
| POST | /errorbook/upload | subject_id, knowledge_point, tags, error_reason, file | 保存原图到 data/uploads/ → OCR 识别 → 切分题目/答案 → 入库 → 发放 points_errorbook |
| POST | /errorbook/update/{id} | question_text, answer_text, error_reason, knowledge_point, tags, status | 编辑；状态改为 mastered 且原状态非 mastered 时发放 points_error_mastered |
| POST | /errorbook/delete/{id} | | 删除记录和图片 |
| GET | /errorbook/export | subject_id, knowledge, tag | 按当前筛选导出错题本 PDF（FileResponse） |
| GET | /errorbook/image/{name} | | 查看原图（防路径穿越：仅 basename） |

OCR 处理流程：
1. 接收图片 → 转灰度 → 增强对比度（阈值140二值化）
2. `tesseract image_to_string(lang="chi_sim+eng", config="--psm 6")`
3. 清理空行，截断 2000 字符
4. 启发式切分：按「答案：/答：/【答案】/参考答案」等标记将题目与答案分开
5. OCR 失败时返回占位提示，题目文字由家长在编辑页手工补录

PDF 导出（ReportLab）：
- 中文字体：优先 `/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc`（Docker），Windows 回退 msyh.ttc / simhei.ttf
- 布局：标题「错题本」+ 导出日期 + 每题（序号、科目、知识点、状态、题目、答案、错因）
- 文件：`data/exports/错题本_YYYY-MM-DD.pdf`

### 5.6 积分奖励（F4/F5/F7）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /rewards/ | | 成长树（等级=积分//100+1）、勋章墙、积分余额、可兑换分钟数、兑换记录、积分明细；顺带检查勋章 |
| POST | /rewards/exchange | minutes, note | 校验积分充足，扣分 + 生成订单；消耗 = minutes × exchange_points |
| POST | /rewards/order/{id}/used | | 标记游戏时间已使用 |
| POST | /rewards/adjust | amount, reason | 人工调整积分（用于奖励特殊表现/纠错） |
| POST | /rewards/refresh-badges | | 手动触发勋章检查 |

勋章检查函数（可复用）：计算连续打卡天数（从今天往前数）、掌握错题数、累计积分，发放满足条件的未获得勋章。

### 5.7 数据统计（F6）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /stats/ | period=week/month/7d | 周报/月报/近7天：完成率、解决难点数、掌握错题数、获得积分、分模块统计、近30天积分趋势 |
| POST | /stats/report | period | 切换统计周期 |

周报数据结构：
- 起止日期、覆盖天数
- 分模块（homework/preview/extra）：total/done/partial/undone/points
- 解决难点数、掌握错题数、积分净额

趋势图：近30天每日净积分柱状图（正数绿色、负数红色），服务端渲染 div 高度。

### 5.8 设置（F4 规则/F8/F9/学生信息）
| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | /settings/ | | 规则表、推送配置、账号管理、学生信息、科目管理 |
| POST | /settings/config | key, value | 更新积分规则值 |
| POST | /settings/notify | reminder_time, reminder_enabled, notify_wecom_webhook, notify_wechat_webhook, notify_email_smtp, notify_email_port, notify_email_user, notify_email_pass, notify_email_to | 保存推送配置，重排定时任务 |
| POST | /settings/test-notify | | 发测试消息，结果带 msg 参数回显 |
| POST | /settings/parent/add | username, password, nickname, subjects | 仅 admin |
| POST | /settings/parent/delete/{id} | | 仅 admin，不能删自己 |
| POST | /settings/password | old_password, new_password | 修改自己的密码 |
| POST | /settings/student | name, grade, gender | 更新学生信息并同步 settings |
| POST | /settings/subject/add | name, color | 新增科目 |
| POST | /settings/subject/delete/{id} | | 删除科目 |

### 5.9 孩子端展示页（F10）
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /kid | 公开（不校验登录）：孩子头像/姓名/年级、积分余额、兑换规则、成长树等级+进度条、勋章墙、今日完成数。页面底部有家长入口链接 |

### 5.10 定时提醒（F9）
- 触发：APScheduler 每日 `reminder_time` 执行（默认 19:00）
- 逻辑：检查当天是否有未完成（status != done）打卡记录；无则跳过；有则推送：
  ```
  【学习小站提醒】孩子今天还有 N 项任务未完成：
  · 校内作业·数学
  · 预习·英语
  快去和孩子一起完成打卡吧~
  ```
- 推送渠道按已配置的 settings 全部执行：企业微信 webhook、微信群 webhook、邮件 SMTP
- 网页内提醒：Dashboard 顶部横幅（当日有未完成时显示）

---

## 6. 前端页面设计（iOS 风格设计系统）

### 6.0 设计方向与记忆点

**设计方向**：「深夜书房」——像 iOS 系统设置与备忘录一样干净、克制、有温度的学习记录体验。
**记忆点**：一眼可辨的 iOS 分组列表（inset grouped list）+ 柔和毛玻璃顶栏 + 有呼吸感的留白。
**差异化原则**：
- 家长端克制专业（浅灰分组背景、系统蓝点缀），孩子端温暖活泼（柔和渐变 + 大圆角 + emoji 视觉）
- 拒绝"AI 通用审美"：不用纯黑纯白、不用紫蓝渐变霓虹、不用圆角矩形硬投影、不滥用玻璃拟态（毛玻璃仅限顶栏与浮动操作按钮）
- 所有灰色带品牌色微色调（chroma 0.01），避免死灰

### 6.1 设计 Tokens

#### 6.1.1 色彩（iOS 语义色体系）
CSS 变量定义，明暗双模式通过 `light-dark()` 与 `prefers-color-scheme` 切换。

| Token | 浅色值 | 暗色值 | 用途 |
|---|---|---|---|
| `--bg` | `#F2F2F7`（iOS 系统灰六） | `#0B0B0F` | 页面背景（不用纯白/纯黑） |
| `--surface` | `rgba(255,255,255,0.72)` | `rgba(28,28,30,0.72)` | 卡片/分组表（半透明，叠在背景上形成层次） |
| `--surface-solid` | `#FFFFFF` | `#1C1C1E` | 需要不透明的控件 |
| `--tint` | `#007AFF`（iOS 系统蓝） | `#0A84FF` | 主操作、链接、当前态 |
| `--tint-press` | `#0055C4` | `#5EC2FF` | 主操作按压态 |
| `--green` | `#34C759` | `#30D158` | 完成/正向 |
| `--red` | `#FF3B30` | `#FF453A` | 扣减/删除/未完成 |
| `--orange` | `#FF9500` | `#FF9F0A` | 提醒/警示 |
| `--yellow` | `#FFCC00` | `#FFD60A` | 星星/勋章/奖励 |
| `--purple` | `#AF52DE` | `#BF5AF2` | 孩子端装饰 |
| `--label` | `#1C1C1E` | `#F2F2F7` | 主文字 |
| `--label-2` | `rgba(60,60,67,0.6)` | `rgba(235,235,245,0.6)` | 次级文字 |
| `--label-3` | `rgba(60,60,67,0.3)` | `rgba(235,235,245,0.3)` | 占位/弱化 |
| `--separator` | `rgba(60,60,67,0.12)` | `rgba(84,84,88,0.4)` | 分组行分隔线（隐藏式弱化） |
| `--shadow` | `rgba(0,0,0,0.06)` | `rgba(0,0,0,0.5)` | 柔和阴影色 |
| `--backdrop` | `rgba(249,249,249,0.72)` | `rgba(22,22,24,0.72)` | 毛玻璃层 |

科目标签色板（预置 8 色，柔和化处理）：
语文 `#E8543F` / 数学 `#0071E3` / 英语 `#3AA757` / 科学 `#7A5AF8` / 道法 `#B15CE6` / 历史 `#C77A1F` / 地理 `#2A9D8F` / 生物 `#4FAE5A`。

#### 6.1.2 字体（系统字体栈，iOS 原生感）
```css
--font: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text",
        "PingFang SC", "HarmonyOS Sans SC", "Noto Sans CJK SC", "Microsoft YaHei", sans-serif;
```
禁止使用 Inter/Roboto/Arial/Open Sans 等非系统字体。

字号阶梯（iOS 风格）：
| Token | 尺寸/字重 | 用途 |
|---|---|---|
| `--fs-large-title` | 34px / 700 | 页面大标题 |
| `--fs-title1` | 28px / 700 | 区块标题 |
| `--fs-title2` | 22px / 700 | 卡片标题 |
| `--fs-title3` | 20px / 600 | 子标题 |
| `--fs-headline` | 17px / 600 | 行标题 |
| `--fs-body` | 17px / 400 | 正文 |
| `--fs-subhead` | 15px / 400 | 次级正文 |
| `--fs-footnote` | 13px / 400 | 辅助说明 |
| `--fs-caption` | 11px / 500 | 徽章/标签 |
数字/积分一律 `font-variant-numeric: tabular-nums` 等宽数字，对齐整齐。

#### 6.1.3 间距（4pt 基准）
`--sp-1: 4px; --sp-2: 8px; --sp-3: 12px; --sp-4: 16px; --sp-5: 20px; --sp-6: 24px; --sp-8: 32px; --sp-10: 40px;`
页面左右安全边距：移动端 16px，桌面 24px；内容最大宽度 1080px 居中。

#### 6.1.4 圆角
`--r-sm: 8px`（输入框/小按钮）、`--r-md: 12px`（按钮/徽章）、`--r-lg: 16px`（卡片）、`--r-xl: 22px`（大卡/弹窗）、`--r-pill: 999px`（胶囊按钮/标签）。
卡片一律大圆角，分组列表用 `--r-lg` 包裹。

#### 6.1.5 阴影（柔和多层，不用硬边框）
```css
--shadow-card: 0 1px 2px var(--shadow), 0 8px 24px var(--shadow);
--shadow-float: 0 2px 8px var(--shadow), 0 16px 40px var(--shadow);
```
卡片分隔优先用背景色差 + 留白（Gestalt 邻近原则），分隔线仅作弱化辅助（`--separator`，1px、50% 透明）。

#### 6.1.6 动效（iOS 节奏）
| 场景 | 参数 |
|---|---|
| 页面进入 | 淡入 + 上移 12px，300ms，`cubic-bezier(0.32, 0.72, 0, 1)`（ease-out-quart 近似） |
| 卡片 hover | 上浮 2px + 阴影增强，200ms ease-out |
| 按钮 active | 缩放 0.97，100ms 即时反馈 |
| 数字变化 | 仅 opacity 过渡 300ms |
| 弹窗 | 淡入 + 缩放下沉（scale 0.95→1），250ms |
| 尊重系统 | 全局 `@media (prefers-reduced-motion: reduce)` 关闭动效 |

只动 `transform` / `opacity`，禁止动画布局属性。

#### 6.1.7 无障碍与状态（硬性）
- 所有可交互元素 5 态齐全：default / hover / active / focus-visible / disabled
- focus-visible：`outline: 3px solid color-mix(in srgb, var(--tint) 40%, transparent)`，圆角跟随元素
- 正文对比度 ≥ 4.5:1（WCAG AA）；触控目标 ≥ 44×44px
- 按钮 disabled：`opacity: 0.4` + 禁止指针事件

### 6.2 全局组件规范

#### 6.2.1 顶栏（base.html）
- 固定顶部，**毛玻璃**：`position: sticky; top: 0; backdrop-filter: blur(20px) saturate(180%); background: var(--backdrop);`
- 布局：左侧品牌「📚 学习小站」（title3 粗体），中央导航（桌面显示，胶囊形当前页高亮 `background: var(--tint); color: #fff`），右侧用户胶囊（头像 emoji + 昵称 + 退出图标）
- 移动端导航收进底部 Tab Bar（iOS 风格，5 个主入口：概览/打卡/错题/奖励/设置，毛玻璃底栏，当前项系统蓝 + 图标）

#### 6.2.2 分组列表（核心视觉组件）
仿 iOS Settings 的 inset grouped list：
```
<section class="group">
  <h2 class="group-title">今日打卡</h2>
  <div class="group-card">
    <div class="row">…</div>
    <div class="row">…</div>   <!-- 行间 1px separator，两端缩进 16px -->
  </div>
</section>
```
- 卡片内行布局：左图标（emoji 圆底 36px）→ 标题（headline）+ 副标题（subhead, label-2）→ 右侧值/操作
- 点击行：`background: rgba(0,122,255,0.08)` 覆盖反馈
- 卡片之间间距 24px，不用嵌套卡片

#### 6.2.3 按钮
- 主按钮：系统蓝填充、`--r-md`、高 44px、文字 17px 600、active 缩放 0.97
- 次要按钮：`--surface-solid` + 1px 弱边框 + label 文字
- 危险按钮：系统红文字版（无填充）
- 图标按钮：44×44 触控区
- 胶囊按钮：`--r-pill`，用于筛选、状态切换

#### 6.2.4 表单控件
- 输入框：iOS 圆角矩形输入（`--r-sm`，高 40px，`--surface-solid`，focus 时 2px 系统蓝描边 + 外发光）
- 下拉（select）：原生样式 + 自定义箭头，高 40px
- 开关（toggle）：iOS 风格 `input[type=checkbox]` 伪装，宽 51px 高 31px，开=系统蓝，带滑块动画
- 日期选择器：`input[type=date]` 原生，iOS 移动端自动唤起滚轮
- 星级评分：5 颗 emoji ⭐，点击高亮，active 弹性缩放

#### 6.2.5 徽章（Status Badge）
| 状态 | 样式 |
|---|---|
| 完成 done / 已解决 solved / 已掌握 mastered | 系统绿浅底 + 绿字胶囊 |
| 部分完成 partial / 已订正 reviewed | 系统橙浅底 + 橙字胶囊 |
| 未完成 undone / 未解决 open / 待订正 pending | 系统红浅底 + 红字胶囊 |
浅底 = `color-mix(in srgb, 对应色 12%, var(--surface-solid))`，字 = 对应色，`--r-pill`，11px 500。

#### 6.2.6 弹窗（Modal）
- iOS 底部弹窗（移动端）/ 居中卡片（桌面），`--r-xl`，毛玻璃遮罩 `backdrop-filter: blur(8px)`
- 标题 headline + 表单 + 底部双按钮（取消=次要、确认=主色），确认按钮 loading 态禁用防重复提交
- 仅必要场景使用（新增/编辑/兑换/确认删除）

#### 6.2.7 空 / 加载 / 错误状态（必须设计）
- **空状态**：emoji 图标（44px）+ 一句鼓励文案 + 一个引导动作按钮。例：错题为空 →「🎉 还没有错题，继续保持！」+「拍照上传第一道错题」
- **加载**：页面骨架屏（skeleton 块闪烁，`prefers-reduced-motion` 下禁用），不转菊花
- **错误**：顶部横幅（系统橙底白字或红底）+ 人类语言说明 + 可操作下一步，不暴露堆栈
- **表单校验**：字段下方 13px 红字提示，focus 保持，不弹 alert

#### 6.2.8 数据表格（桌面宽屏）
- 卡片容器 + 行 hover 浅灰、行间弱分隔线；移动端自动降级为卡片流
- 数字列右对齐 tabular-nums；状态徽章列居中

### 6.3 响应式与暗色模式

- **断点**：`<768px` 移动（单列 + 底部 Tab Bar）、`768-1080px` 平板（双列）、`>1080px` 桌面（多列网格）
- **暗色模式**：CSS 变量切换，阴影/描边/毛玻璃全部随变量走；`<meta name="color-scheme" content="light dark">`
- 登录页/孩子端始终亮色氛围（孩子使用平板场景），家长端跟随系统

### 6.4 顶栏导航（base.html）
见 6.2.1。所有页面继承：毛玻璃顶栏 + 大标题区（`large-title` 页面名 + 日期/副标题）+ 内容流。

### 6.5 登录页（login.html）
- 全屏柔和渐变背景（`#F2F2F7 → #E8F0FE`，iOS 奶油感，非霓虹）
- 居中卡片：📚 品牌大图标（64px emoji 圆底）+「学习小站」大标题 + 标语「和孩子一起，把学习变成一件有成就感的事」（label-2）
- 表单：用户名/密码（圆角输入框）+ 系统蓝主按钮
- 底部 footnote：「首次使用：admin / admin123」+ 孩子端入口链接
- 错误提示：输入框上方红字横幅

### 6.6 今日概览（dashboard.html）
- 顶部毛玻璃大标题区：日期 +「今天也要加油呀」副标题
- **提醒横幅**（有未完成时）：橙色渐变卡片（`--orange` 12% 底），胶囊图标 + 文案 +「去打卡」按钮
- **统计四宫格**（2×2，桌面 4 列）：积分余额（⭐ 黄色图标）、未解决难点、待订正错题、兑换规则（1 分钟=N 星），每格 = 大数字 title1 + 标签 caption
- **左主区**：今日打卡三个分组列表（校内作业/预习/课外练习），行内状态徽章 + 质量星 + 积分（`+10 ⭐` 绿色 / `-5 ⭐` 红色）
- **右侧栏**：最近积分流水分组列表（emoji 图标 + 说明 + 时间 caption + 变动金额）；空态引导

### 6.7 每日打卡（daily.html）
- 顶部：日期切换（左右箭头 + 日期胶囊，今天高亮）
- 三个分组卡片（校内作业/预习/课外练习），每组右上角「+」圆形图标按钮（44px，active 旋转 90°）
- 记录行：科目标签色点 → 内容（body）→ 状态徽章 → 质量星 → 积分 → 删除（滑动显示/长按弹确认，桌面 hover 显示）
- 新增/编辑弹窗：科目（胶囊单选）、内容（textarea）、状态（三个胶囊单选：完成/部分完成/未完成）、质量（星级）；保存后行内容淡入更新

### 6.8 难点疑点（difficulty.html）
- 顶部「未解决 / 已解决」分段控制器（iOS segmented control：浅底胶囊容器 + 白色滑块，选中文字系统蓝）+「+ 记录难点」主按钮
- 列表为分组卡片行：科目色点 + 标题（headline）+ 详情摘要（subhead 单行截断）+ 状态徽章 + 时间
- 标记解决弹窗：说明已解锁「+15 ⭐」绿色提示条 + solution 输入框

### 6.9 错题本（errorbook.html）
- 顶部操作行：主按钮「📷 拍照上传错题」+ 次要按钮「导出 PDF」
- 筛选行：科目下拉、知识点输入、标签输入、状态下拉 +「筛选」胶囊按钮，筛选条件激活时显示清除按钮
- 列表分组卡片行：科目色点 + 题目摘要（body，2 行截断，点击缩略图可查看原图 lightbox）+ 知识点标签（浅灰胶囊）+ 状态徽章 + 操作（编辑/删除）
- 上传弹窗：文件选择区（虚线圆角框，拖拽/点击，选后显示缩略图）+ 科目胶囊 + 知识点 + 标签 + 错因；提交后 OCR loading 提示「正在识别题目…」（骨架屏）
- 编辑弹窗：题目/答案 textarea + 知识点 + 标签 + 错因 + 状态三段控制器（待订正/已订正/已掌握），切到已掌握显示「+15 ⭐」提示

### 6.10 积分奖励（rewards.html）
- **成长树区**：iOS 卡片，背景柔和绿渐变（12% 绿），emoji 🌳 随等级放大（Lv1-3 小苗 / Lv4-6 小树 / Lv7+ 大树），等级大数字 title1 + 进度条（胶囊形，绿色填充，圆头）+「距 Lv N 还差 X 星」footnote
- **勋章墙**：badge-grid（自动流式网格，每个勋章 = emoji 圆底 56px + 名称 caption；未获得 = 30% 透明 + 灰阶 + 锁形占位）
- **兑换卡**：积分余额大数字 +「可兑换 N 分钟」+ 兑换表单（分钟数 input + 备注 + 主按钮）；兑换记录分组列表（分钟 + 状态徽章「待使用/已使用」+ 标记已使用操作）
- **积分明细**：底部分组列表（emoji 图标 + reason + 时间 caption + 金额，正绿负红 tabular-nums）

### 6.11 数据统计（stats.html）
- 顶部：分段控制器（本周 / 本月 / 近7天）
- 统计四宫格：完成率（环形进度 SVG，绿）、解决难点、掌握错题、获得积分
- 分模块统计：分组列表三行（校内作业/预习/课外练习），每行显示完成比例进度条 + 次数 + 积分
- 趋势图：近30天柱状图（服务端渲染 div 高度，柱顶圆角 4px，正绿负红，底部日期 caption；hover 显示数值 tooltip）

### 6.12 设置（settings.html）
- 三列分组（桌面）/ 单列（移动）
- **积分规则组**：分组列表行 = 规则名（headline）+ 说明（footnote）+ 分值输入框（宽 72px 右对齐，正数前缀 + 绿/负数前缀 - 红）+ 行内保存按钮；修改后 toast「已保存」
- **提醒推送组**：开关行（每日提醒开关）+ 时间行（date 控件）+ webhook/邮件配置行（输入框）+「发送测试消息」按钮 + 测试结果横幅
- **家长账号组**：账号行（昵称 + 用户名 + 角色徽章 + 删除）+「添加家长」行（点击展开内联表单）
- **学生信息组**：姓名/年级/性别行（行内编辑）
- **科目管理组**：科目行（色点 + 名 + 删除）+「添加科目」行

### 6.13 孩子端（kid.html，公开）
- 移动优先大字号，柔和暖色渐变背景（`#FFF7E6 → #FFE8F0`，非霓虹）
- 顶部：孩子头像（大 emoji 圆底 96px）+ 姓名 + 年级（title1 粗体）+ 今日完成数徽章
- 成长树大卡：放大版 emoji + 等级 + 进度条 +「再得 X 星升到 Lv N」
- 勋章墙：同 6.10，但徽章更大（64px）
- 底部：兑换规则卡（「3 ⭐ = 1 分钟游戏时间」大字）+ 最近获得的奖励流水（最近 5 条，开心文案）
- 页面底部小字「家长入口 →」（跳 /login）

### 6.14 样式实现要点（static/css/style.css）
- 单文件 CSS，按 6.1 tokens 组织；所有颜色走 CSS 变量
- 用 `light-dark(var(--x-light), var(--x-dark))` + `@media (prefers-color-scheme: dark)` 双保险
- 组件类命名：`.group/.group-title/.row/.badge/.btn/.btn-primary/.btn-secondary/.segmented/.toggle/.input/.modal/.toast/.skeleton`
- 动效用 `@keyframes fadeUp` 等；`prefers-reduced-motion` 全局关闭
- 交付时浏览器实测（Chrome/Edge/Safari），验证深浅色、各断点、无横向滚动

---

## 7. 安全与数据保护

1. 密码：PBKDF2-SHA256（100000 轮，固定盐 studyhub，家庭场景足够）
2. 会话：HttpOnly Cookie，7 天过期
3. 权限：/settings 中账号管理仅 admin；其余功能家长均可操作
4. 文件安全：上传图片用随机文件名（`err_YYYYMMDD_<uid>_<hex>.jpg`）；查看图片仅取 basename，防路径穿越
5. 删除保护：业务数据删除为物理删除（家庭单机场景可接受），删除前可确认
6. 数据持久化：全部在 data/ 卷，Docker 升级不丢数据
7. 提示：首次登录后修改默认密码；NAS 若暴露公网建议加反向代理 + HTTPS

---

## 8. Docker 部署（群晖 NAS）

### 8.1 docker-compose.yml
```yaml
version: "3.8"

services:
  studyhub:
    build: .
    container_name: studyhub
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
```

### 8.2 Dockerfile 要点
```dockerfile
FROM python:3.11-slim
ENV PYTHONUNBUFFERED=1 TZ=Asia/Shanghai
RUN apt-get update && apt-get install -y --no-install-recommends \
    tesseract-ocr tesseract-ocr-chi-sim fonts-noto-cjk tzdata \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app ./app
COPY data ./data
EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 8.3 部署步骤（Container Manager）
1. 项目目录上传 NAS：`/volume1/docker/studyhub/`
2. Container Manager → 项目 → 新增 → 选择 docker-compose.yml
3. 启动，等待构建完成（首次约 3-5 分钟）
4. 访问 `http://NAS的IP:8000`

### 8.4 访问入口
- 家长端：`http://NAS的IP:8000/dashboard`
- 孩子端：`http://NAS的IP:8000/kid`
- 外网：路由器端口转发 + DDNS，或群晖 QuickConnect

---

## 9. 验收标准

### 9.1 功能验收
| 编号 | 验收项 | 通过标准 |
|---|---|---|
| A1 | 首次启动 | 自动建库建表，admin/admin123 可登录，默认科目/规则已初始化 |
| A2 | 每日打卡 | 可增删改三类记录；完成后积分自动入账；重复添加同科目同类别提示/更新；质量5星额外加分；未完成扣分 |
| A3 | 难点疑点 | 新增/解决/重开/删除正常；解决后 +15 星并写流水 |
| A4 | 错题上传 | 中文图片可识别出文字；原图可查看；未识别时可在编辑页补录 |
| A5 | 错题筛选 | 按科目/知识点/标签/状态筛选正确 |
| A6 | 错题导出 | 导出 PDF 中文正常显示，含筛选范围题目 |
| A7 | 兑换 | 积分充足可兑换；不足提示；扣分正确；订单可标记已使用 |
| A8 | 勋章 | 满足条件自动发放（连续打卡/错题/积分） |
| A9 | 统计 | 周/月报数据正确，趋势图展示 |
| A10 | 推送 | 配置企业微信 webhook 后测试消息可达；每日 19:00 未完成提醒推送 |
| A11 | 多家长 | 可添加第二个账号并正常登录使用 |
| A12 | 孩子端 | /kid 无需登录可访问 |
| A13 | 数据持久化 | 重启容器后数据完整 |
| A14 | iOS 视觉 | 页面使用 6.1 设计 tokens（色彩/字体/间距/圆角/阴影），浅色背景 #F2F2F7 分组列表结构，无纯黑纯白 |
| A15 | 毛玻璃顶栏 | 顶栏/底部 Tab 使用 backdrop-filter blur+saturate，滚动时内容穿透可见 |
| A16 | 深浅色模式 | 跟随系统切换暗色，文字/阴影/毛玻璃全部随变量适配，无刺眼高亮 |
| A17 | 响应式 | 手机（单列+底部 Tab）、平板（双列）、桌面（多列）三档均无横向滚动，触控目标 ≥44px |
| A18 | 动效与状态 | 页面淡入、按钮 active 缩放、hover 上浮；prefers-reduced-motion 下全部停用 |
| A19 | 组件五态 | 所有按钮/行/输入框具备 default/hover/active/focus-visible/disabled 五态，focus 环可见 |
| A20 | 空/加载/错误态 | 各列表空状态有 emoji+文案+动作按钮；加载用骨架屏；表单校验红字提示 |

### 9.2 性能验收
- 家庭单机并发场景（≤5 人）各页面响应 < 2 秒
- OCR 单张图片处理 < 10 秒

---

## 10. 后续扩展建议（v2 可选）
1. 家长分工权限隔离（按科目限制可操作范围）
2. 多孩子支持（切换孩子）
3. 错题复习模式（随机抽题）
4. 游戏时间联动设备限制（如 Windows 家长控制）
5. 周报自动推送（每周日推送 PDF/文字总结）
6. HTTPS 与反向代理示例（Caddy/Traefik）
7. 数据备份脚本（定时复制 data/ 到 NAS 备份目录）
*（内容由AI生成，仅供参考）*
